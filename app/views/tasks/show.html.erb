<h1>MY TASKS DETAILS 📝</h1>

<p><%= @task.details %></p>

<%= link_to "Edit task", edit_path %>
<%= link_to "Delete #{@task.title}",
  task_path(@task),
  method: :delete,
  data: { confirm: "Are you sure?" } %>
